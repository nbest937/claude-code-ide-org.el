.claude/settings.local.json
.claude/worktrees/
clock-status.json
.claude-code-ide-org-audit.jsonl

# Byte-compiled elisp: `load' prefers a newer .elc, and git restores no
# mtime ordering, so a tracked one can silently shadow a config.el edit.
*.elc
