#+TODO: TODO NEXT DOING WAIT MAYBE | DONE CANCELLED
#+TAGS: code comms research review
#+ARCHIVE: DONE.org::* Done

* DONE Set git identity :code:
:PROPERTIES:
:ID:       51591672-95b9-4092-a037-af7b3fe45fbc
:END:
Blocking the very first commit of this repo — everything is staged
and ready, git just doesn't know who you are yet. Run, in the repo:
#+begin_src sh
git config user.name "Your Name"
git config user.email "nbest937@gmail.com"
#+end_src
Then ask Claude to finish the commit it already staged.

* DONE Consolidate historical SESSIONS/LOGBOOK entries :code:
:PROPERTIES:
:CREATED:  [2026-07-28 Tue 13:01]
:ID:       ccdff985-b206-4a8d-a6d2-9bc01cfdda3a
:END:
:SESSIONS:
- Resumed [2026-07-28 Tue 13:03]
- Paused [2026-07-28 Tue 15:58]
- Resumed [2026-07-28 Tue 16:08]
- Paused [2026-07-28 Tue 16:09]
- Resumed [2026-07-28 Tue 16:16]
- Paused [2026-07-28 Tue 16:17]
- Resumed [2026-07-28 Tue 16:24]
- Paused [2026-07-28 Tue 16:25]
- Resumed [2026-07-28 Tue 16:25]
- Paused [2026-07-28 Tue 16:25]
- Resumed [2026-07-28 Tue 16:29]
- Paused [2026-07-28 Tue 16:32]
- Resumed [2026-07-28 Tue 16:33]
- Paused [2026-07-28 Tue 16:33]
- Resumed [2026-07-28 Tue 16:40]
- Paused [2026-07-28 Tue 16:40]
:END:
:LOGBOOK:
CLOCK: [2026-07-28 Tue 16:29]--[2026-07-28 Tue 16:32] =>  0:03
CLOCK: [2026-07-28 Tue 16:24]--[2026-07-28 Tue 16:25] =>  0:01
CLOCK: [2026-07-28 Tue 16:16]--[2026-07-28 Tue 16:17] =>  0:01
CLOCK: [2026-07-28 Tue 16:08]--[2026-07-28 Tue 16:09] =>  0:01
CLOCK: [2026-07-28 Tue 13:45]--[2026-07-28 Tue 14:00] =>  0:15
CLOCK: [2026-07-28 Tue 13:05]--[2026-07-28 Tue 13:15] =>  0:10
:END:
One-time retrospective cleanup of the per-turn SESSIONS/LOGBOOK churn
accumulated so far: collapse each heading's :SESSIONS: entries to one
min-to-max span per calendar day; round each :LOGBOOK: CLOCK interval
to the nearest 5-minute multiple; then, after sorting, merge any
intervals that are adjacent or overlapping post-rounding into a single
min-to-max span. Implemented as a reusable elisp function rather than a
throwaway script, so the same logic can back the on-the-fly version
scoped separately — see "Auto-consolidate SESSIONS/LOGBOOK on the fly"
under Clock lifecycle & visibility.

* Roadmap

Brainstormed, not committed to — all MAYBE on purpose. Pick what's
actually worth building and I'll bump it to NEXT. Grouped by what
each cluster is actually for, not by which layer (org vs Claude)
would implement it — several items that live in different systems
turned out to be the same underlying goal.


** Developer tooling

*** TODO Revisit: does org.skill's checkbox discipline apply to TODO.org itself? :research:code:
:PROPERTIES:
:ID:       6a448345-016c-495f-b26b-ee81b30cd95c
:CREATED:  [2026-07-29 Wed 07:42]
:END:
Tonight's parallel-subagent batch (10 MCP tools) left every archived
heading's Plan checkboxes at all-[ ], despite full implementation —
org.skill's existing "check off as you go" discipline was never
applied. Fixed retroactively (DONE.org reconciled by hand) plus a new
CLAUDE.md rule making it a coordinator responsibility. But a later
conversation pushed back on the reasoning behind that rule, and it got
meta enough to warrant its own session rather than more tail-end
discussion bolted onto an already-long one.

*Why TODO.org is a special case, not a template for org.skill in
general:* org.skill normally applies to a user's .org files living in
arbitrary other projects/folders — files with no relationship to a
git repo whose code is simultaneously being changed by parallel
subagents. TODO.org here is unusual on two axes at once: (1) it's
this repo's own tracked source file, in the same repo as the code
the subagents are editing, and (2) it's edited through two different
channels — live-Emacs/emacsclient (state, clock, archive) and
git-tracked branch diffs (plan-checkbox text) — that don't normally
have to coexist for an arbitrary end-user org file.

*Where the discussion landed, tentatively, before deferring:* the
real risk this session wasn't "two subagents' diffs collide" (rare —
different headings, same shape as config.el's clean merges) — it was
that the coordinator (this session) batched all TODO.org/DONE.org
commits at the very end instead of committing incrementally per
task, the same cadence used for config.el. That left git's tracked
TODO.org frozen at its pre-session state for the whole session while
the live-Emacs-edited on-disk file kept drifting — a mismatch that
has nothing to do with how many subagents are running or what they
touch. Tentative fix direction: commit TODO.org per-task (not
batched); let a subagent check off its own heading's boxes/decisions
directly in its own worktree; rely on the already-enabled
global-auto-revert-mode to sync the live buffer after a merge before
archiving that heading.

*Open questions for the fresh session:* does this generalize past
this repo's own meta-bookkeeping file, or is it inherently special
enough that any resulting rule belongs only in this repo's own
CLAUDE.md, not in org.skill/org-dev.skill? Should the CLAUDE.md rule
added this session ("coordinator reconciles the plan post-hoc") be
revised in favor of "subagent reconciles its own heading, coordinator
just commits incrementally") ? What's the actual failure mode if
auto-revert doesn't fire before an archive call — does it need an
explicit revert-buffer check rather than an assumption?


Doesn't fit any theme below — this is about maintaining *this
repo's own code*, not about org-mode capability. Its own group
because it's genuinely a different kind of work.

*** DONE org-dev skill :code:
:PROPERTIES:
:ID:       e1e60e00-e7b7-4e62-88c3-55a936fdece7
:CREATED:  [2026-07-28 Tue 10:18]
:END:
:SESSIONS:
- Resumed [2026-07-28 Tue 10:53]
- Paused [2026-07-28 Tue 13:01] (recovered)
:END:
:LOGBOOK:
CLOCK: [2026-07-28 Tue 12:35]--[2026-07-28 Tue 13:00] =>  0:25
CLOCK: [2026-07-28 Tue 11:35]--[2026-07-28 Tue 12:05] =>  0:30
CLOCK: [2026-07-28 Tue 11:25]--[2026-07-28 Tue 11:30] =>  0:05
CLOCK: [2026-07-28 Tue 11:15]--[2026-07-28 Tue 11:20] =>  0:05
CLOCK: [2026-07-28 Tue 10:55]--[2026-07-28 Tue 11:00] =>  0:05
:END:
A development-tooling skill packaging the reload-and-verify
procedure this session had to rediscover by hand, more than once:
the =after!=/hook-ordering bug that only surfaced after a real
Emacs restart, =bin/test= silently running against the wrong
bundled org version for a while, and =org.skill='s zip needing
unzip/edit/rezip rather than a direct text edit. Different from
=org.skill= — that one is for editing =.org= file *content* for end
users; this one is for reloading and verifying changes to *this
project's own code* (config.el, hooks, bin/test).

**** Plan [8/8]
- [X] Package as =org-dev.skill= (zip) at the repo root containing
  =org-dev/SKILL.md=, matching =org.skill='s exact convention — frontmatter with
  =name=/=description= including trigger conditions
- [X] Document the Emacs-side reload procedure: =emacsclient -e
  '(load-file "modules/tools/claude-code-ide-org/config.el")'= for a
  live instance
- [X] Document when a live reload isn't enough and a full Emacs
  restart is required instead: anything touching =after!=/
  =with-eval-after-load=/hook-registration-at-load-time logic — the
  =org-clock-load= bug this session hit is the concrete example
  (documented via CLAUDE.md's design notes prose; there is no
  =config.el:249= line reference — confirmed absent from the repo)
- [X] Document post-restart re-verification (not redoing — the
  Warp-wiring block already auto-runs at Doom startup): confirm the
  pinned MCP port, =warp= session, and IDE-companion lockfile came
  back up, e.g. =emacsclient -e '(claude-code-ide-mcp-server-get-port)'=
- [X] Document the sanity-check pattern after any reload:
  =emacsclient -e '(fboundp (quote newly-added-function))'=, matching
  the verify-don't-trust pattern used throughout this session
- [X] Document the Claude-Code-hook-side limit explicitly: editing
  =.claude/settings.json= never takes effect in the *current* session
  (hook config is read once at session start) — the skill can only
  detect this and tell the user a new session is needed, it cannot
  force one
- [X] Document the =bin/test= org-version-pinning gotcha as a
  troubleshooting pointer: if a function seems unexpectedly void or
  tests behave strangely, confirm =bin/test= is actually loading the
  straight-managed org, not Emacs's bundled one
- [X] Document the =org.skill= zip-packaging gotcha: editing skill
  content means unzip → edit =SKILL.md= → rezip preserving the
  internal =org-mode/SKILL.md= path, never editing the =.skill= file
  directly (it's binary)
- [X] Decide the trigger condition precisely: auto-invoke whenever
  config.el/config-test.el/the Doom config has just been edited in
  session, or only on explicit request ("reload", "verify this
  works", "test in Emacs") — decided both: the skill description
  triggers on either signal

*Scope limit, decided already:* the Emacs-reload half can be fully
automated (deterministic, safe to just do). The Claude-Code-hook half
can only be *flagged*, never performed — nothing running inside a
session can force that same session to restart. The honest shape is
"auto-reload Emacs, then tell the user a new session is needed if
=.claude/settings.json= changed," not a single button that does
everything.

*Follow-up:* [2/2] — per the new "test new features" rule in CLAUDE.md
- [X] Content-accuracy check: =bin/check-org-dev-skill= (fish), a
  doctest for the skill in the same spirit as =bin/test= catching
  drift in the wrapper functions. Asserts, against live state rather
  than parsing the fences generically: =emacsclient -e
  '(claude-code-ide-mcp-server-get-port)'= returns =45571= and matches
  =.mcp.json=; =org.skill=/=org-dev.skill= each contain exactly one
  zip entry at the documented internal path; =bin/test='s =build-*=
  glob resolves to a directory containing =org/=; and the
  =config.el:410=/=config.el:457= line references cited in
  =org-dev/SKILL.md= still point at the tool-registration block and
  file end. All checks pass as of [2026-07-28 Tue].
- [X] Explicitly out of scope for automation: whether the skill
  actually *triggers* on its description's trigger phrases — that's
  semantic matching decided fresh each session, not deterministic;
  verify by spot-checking fresh sessions against representative
  prompts instead, don't force it into a hard test. (Decision stands
  as-is — no code follow-up needed, recorded here per CLAUDE.md's rule
  to say so explicitly rather than skip silently.)

*** DONE Make check-org-dev-skill line-number checks robust to config.el drift :code:
:PROPERTIES:
:ID:       9be21b6b-5219-4acd-950a-e04b60d8fe1e
:CREATED:  [2026-07-28 Tue 13:56]
:END:
=bin/check-org-dev-skill='s tool-registration-block-start and
last-line checks hardcode absolute =config.el= line numbers (410/457,
then 462/528, now 688/754 after the =consolidate_sessions_logbook=
merge). Every =config.el= edit that changes line count breaks these
two checks again immediately — confirmed twice in one day. Redesign
so the check doesn't hardcode absolute line numbers (e.g. anchor on a
nearby stable string/regex, or compute an expected relationship rather
than a magic number) instead of just re-editing the constants each
time drift is noticed.

*Done, on =feature/robust-check-org-dev-skill-lines=:* the two checks
now extract the expected =NNN-MMM= range straight out of the
=org-dev/SKILL.md= citation text (via =unzip -p= + a regex) instead of
carrying a second hardcoded copy in the script. Single source of truth
is the doc, which already needs a human edit whenever the cited range
moves — this just stops the script from needing its own, independent
edit for the same drift.

*Caveat, precisely:* this is consolidation, not immunity. A
=config.el= edit that shifts these lines will still make the check
FAIL — verified by temporarily inserting blank lines into =config.el=
and confirming both checks fail with a nonzero exit, then restoring
and confirming they pass again. The one remaining edit needed
afterward is to =org-dev/SKILL.md='s citation (already required for
human readers), not also to =bin/check-org-dev-skill=. Dropping the
exact line numbers from the doc entirely (e.g. citing the
=with-eval-after-load= anchor with no numbers) would remove drift
pressure altogether, but that's a doc-content trade-off — it makes the
doc's own reference to readers less precise — left for a separate
decision rather than made unilaterally here.


** New MCP tools

On-demand capabilities Claude would call. Independent of each
other — no shared implementation, just the same "make it a tool
instead of raw text editing" shape.

*** DONE org_query :code:
:PROPERTIES:
:ID:       4ae3cdef-98b5-4361-818e-aef3491f2fb8
:END:
:SESSIONS:
- Resumed [2026-07-28 Tue 12:16]
- Paused [2026-07-28 Tue 12:27]
:END:
Structured search over headings. Biggest ROI item here: most of
what's actually slow today is Claude reading whole files to answer
things a real query would answer in one call — "what's blocked",
"what did I touch this week", "everything :research: and not DONE".
Worth scoping against =org-ql= rather than hand-rolling a query
language.

**** Plan [7/7]
Verified against org-ql's actual source (github.com/alphapapa/org-ql),
not guessed from memory.
- [X] Add =(package! org-ql :recipe (:host github :repo
  "alphapapa/org-ql"))= to packages.el (updated the "no additional
  packages required" note)
- [X] Reuse the existing =claude-code-ide-org-query-files= defcustom
  (already added for stale-interval recovery, doc comment already said
  "org_query once built") via the existing
  =claude-code-ide-org--tracked-files= helper — no new defcustom needed,
  correcting this plan's earlier draft
- [X] Implemented =claude-code-ide-org-query (query)=: parses via
  =(org-ql--query-string-to-sexp query 'and)=, rejecting a nil parse
  before ever calling =org-ql-select= (confirmed
  =org-ql-signal-peg-failure= defaults to nil, so a malformed query
  doesn't signal on its own), then
  =(org-ql-select (claude-code-ide-org--tracked-files) sexp :action
  #'claude-code-ide-org--format-query-match)=
- [X] Documented the plain-string syntax in the tool description:
  =todo:WAIT=, =tags:research,code= (comma = OR), =priority:A=,
  =heading:"text"=, negation with =!= (e.g. =!todo:DONE=)
- [X] Formats each match as one line — state, heading, tags, :ID:,
  file — joined into a single string; distinct string results for
  empty query, unparseable query, and zero matches, matching the
  existing "always return a string" convention
- [X] Registered with a single =query= string arg
- [X] Parser restricted to the string mini-language only — never
  =eval= a model-supplied sexp; org-ql's separate full sexp predicate
  language is never reachable from this tool's input

*Risk, confirmed and accepted:* =org-ql--query-string-to-sexp= is
confirmed still a double-dash "private" function with no public
alternative (dynamically =fset= near the end of org-ql.el at load
time, so it's reliably present after a plain =(require 'org-ql)=) —
the version-drift risk stands as originally flagged, nothing to do
about it now beyond noting it. Separately, org-ql also exposes a full
sexp predicate language — effectively arbitrary elisp execution
against the user's files if exposed. Same class of concern as "Remote
MCP access" below, just reachable locally instead of remotely; this
tool never exposes that path.

*Implementation notes:*
- =bin/test= generalized to prepend /every/ straight-built package's
  directory to load-path (not just org's), since org-ql pulls in
  =peg=/=ts= transitively — avoids hand-listing org-ql's own
  dependency set, which could drift.
- =doom sync= alone did not pick up the new =packages.el= entry during
  implementation: =~/.config/doom/modules/tools/claude-code-ide-org=
  is a symlink to this repo's =main= checkout (see CLAUDE.md's
  "Repository layout"), not to whichever branch/worktree is currently
  being edited, so a feature-branch worktree's =packages.el= change is
  invisible to =doom sync= until it lands on =main=. Installed org-ql
  directly for testing instead: =emacsclient -e "(straight-use-package
  '(org-ql :type git :host github :repo \"alphapapa/org-ql\"))"= —
  same practical effect, sidesteps the symlink. The =packages.el=
  entry remains the source of truth for a plain =doom sync= to pick up
  once this branch reaches =main=.
- All 24 tests pass (18 pre-existing + 6 new). Implemented on branch
  =feature/org_query=; this TODO.org bookkeeping was applied directly
  to =main= instead, per CLAUDE.md's feature-branch rule explicitly
  exempting "small doc/TODO edits."

*** DONE org_clock_report :code:
:PROPERTIES:
:ID:       26215a14-6ec9-43a7-981b-5c8e888e65fd
:BLOCKER:  4ae3cdef-98b5-4361-818e-aef3491f2fb8
:END:
:SESSIONS:
- Resumed [2026-07-29 Wed 02:00]
- Paused [2026-07-29 Wed 02:04]
:END:
:LOGBOOK:
:END:
Summarize time in a date range. Wrap =org-clock-report=/column view
so "what did I work on this week" is one tool call instead of
Claude eyeballing LOGBOOK entries across files by hand.

**** Plan [/]
- [ ] Implement =claude-code-ide-org-clock-report (&optional id block
  tstart tend)=: scope to a subtree if =id= given, else fall back to
  the same file-list variable as org_query
- [ ] Build a clocktable params plist (=:scope=, =:maxlevel=, and
  either =:block= — today/thisweek/lastweek/thismonth — or explicit
  =:tstart=/=:tend=)
- [ ] Call =org-dblock-write:clocktable= in a throwaway temp buffer
  and return the resulting table string — no file created or
  modified
- [ ] Register with optional args =id=, =block= (enum), =tstart=,
  =tend=

*Risk:* low — a known idiom, but unexercised in this codebase;
spot-check before committing to it as the mechanism.

** Enforce the transition rules

CLAUDE.md's transition table (TODO→DOING opens a clock, etc.) is
pure prose right now — nothing stops a hand-edit in Emacs, or some
future tool call, from violating it. These three items are one
project split across two enforcement layers plus the tests that
should cover both: org's own dependency hooks catch violations
regardless of how the state change happened, a Claude-side hook
catches the specific path our own tools take one step earlier, and
neither is done until it's tested.

** Clock lifecycle & visibility

Everything here is specifically about the clock subsystem's
correctness and observability, as opposed to the general TODO-state
machine above — three different angles on "make sure the clock is
never silently wrong."

*** MAYBE Idle-based auto clock-out :code:
Via =org-clock-idle-time=, already built into org — just needs
enabling. Guards against a clock left running because a session
died mid-DOING without ever reaching org_clock_out.

**** Plan [/]
- [ ] Set =org-clock-idle-time= (minutes) in
  =~/.config/doom/config.el='s =after! org= block — a pure
  Doom-config change, nothing in this repo touches it
- [ ] Evaluate =org-clock-auto-clock-resolution= or a small
  non-interactive wrapper around =org-clock-resolve-clock=, since
  org's default resolution on resume is *interactive* and nobody
  answers that prompt in an unattended session
- [ ] Confirm empirically whether the resolve prompt does anything
  useful when Emacs is running unattended, or whether auto-resolution
  is required for this to help at all

No config.el/config-test.el changes — there's no wall-clock idle to
simulate in ERT's scratch-buffer fixtures.

*** MAYBE Coarsen :SESSIONS: to session-level, not turn-level :code:
:PROPERTIES:
:CREATED:  [2026-07-28 Tue 11:06]
:END:
:SESSIONS: currently inherits the same Stop/UserPromptSubmit hooks as
:LOGBOOK:, so every turn boundary writes a Paused/Resumed pair — fine
for CLOCK, whose "many short intervals instead of one long one"
granularity is intentional (see the dual-timekeeping design note), but
not what :SESSIONS: was meant to show: the *session-level* bracketing
arc, not per-prompt noise. Wanted instead: a genuine close only at
DOING → DONE with the work tested and committed, at the end of a
working day, or on an explicit signal — sketched as a command like
=/dfd <task>= ("done for the day").

**** Plan [/]
- [ ] Detach :SESSIONS: writes from the Stop/UserPromptSubmit hooks
  entirely — CLOCK keeps its current per-turn pause/resume behavior
  unchanged; :SESSIONS: stops being written there
- [ ] Design =/dfd <task>=: closes the day's :SESSIONS: entry (and
  clocks out if a clock is running) without changing TODO state — the
  task stays DOING, just paused for the day
- [ ] Handle the DONE+tested+committed close as a standing instruction
  (same shape as the DOING-transition rule in CLAUDE.md), not a hook —
  "tested and committed" is a judgment call nothing mechanical can
  verify
- [ ] Decide what happens to the *first* Resumed entry under the new
  scheme: opened on the first clock-in after a close, or only once
  real multi-day bracketing is worth showing at all

*Open question:* does "end of a working day" need its own detection
(e.g. against =claude-code-ide-org-working-hours=), or is =/dfd=
always a manual call — auto-detecting day boundaries crosses into
guessing user intent, exactly the kind of call CLAUDE.md's existing
rules say only the model/user should make, not a hook.

*** DONE Auto-consolidate SESSIONS/LOGBOOK on the fly :code:
:PROPERTIES:
:CREATED:  [2026-07-28 Tue 13:13]
:ID:       a36d2436-7601-4bf5-a2e7-a4613755156d
:END:
:SESSIONS:
- Resumed [2026-07-28 Tue 16:48]
- Paused [2026-07-28 Tue 17:00] (recovered)
:END:
:LOGBOOK:
CLOCK: [2026-07-28 Tue 16:50]--[2026-07-28 Tue 17:00] =>  0:10
:END:
Companion to the retrospective one-time cleanup ("Consolidate
historical SESSIONS/LOGBOOK entries", done this session): wire the
same rounding/merging logic into =org_clock_out= itself, so the
drawers stay consolidated continuously instead of needing another
manual pass later. Reuses
=claude-code-ide-org-consolidate-history= directly — that function
was deliberately written and tested as a reusable capability, not a
throwaway script, for exactly this.

**** Plan [5/5]
- [X] Call =(claude-code-ide-org-consolidate-history id)= at the end
  of =claude-code-ide-org-clock-out= (and the recovery path in
  =claude-code-ide-org-close-open-interval=), scoped to the heading
  that was just clocked out of — never to a heading that might still
  be actively clocking
- [X] Confirm the ordering is safe: =consolidate-history= already
  leaves an open CLOCK/trailing Resumed completely untouched, but the
  call still must happen *after* =org-clock-out= has actually closed
  the interval, not before — captured the heading's :ID: from
  =org-clock-marker= before calling =(org-clock-out)=, since the
  marker becomes invalid once the clock actually closes
- [X] Idempotency confirmed empirically (a manual =consolidate-
  history= call right after clock-out reports "Nothing to
  consolidate" — the two new on-the-fly ERT tests assert exactly
  this), but *not* via the incremental "only re-examine today's
  entries and the newest CLOCK line" scheme this bullet originally
  proposed. Implemented instead as a full rescan of the drawer on
  every clock-out. Simpler, and "cheap" holds regardless — these
  drawers are a handful of lines at most, so a full regex rescan
  costs nothing measurable. The incremental version remains available
  as a future optimization if a heading's history ever grows large
  enough for that to matter, which hasn't happened in practice.
- [X] Add ERT tests: =claude-code-ide-org-test-clock-out-consolidates-
  on-the-fly= and =claude-code-ide-org-test-close-open-interval-
  consolidates-on-the-fly=, both verifying the "manual recheck is a
  no-op" idempotency property rather than asserting exact rounded
  values, which would depend on unpredictable wall-clock timing
- [X] Kept the same rounding convention as the retrospective pass —
  literally reuses =claude-code-ide-org--round-time-to-5-minutes= and
  =claude-code-ide-org--merge-time-intervals= via the same
  =consolidate-history= entry point, not a second implementation

*Relationship to "Coarsen :SESSIONS: to session-level" above:*
different mechanism, same underlying pain. That proposal changes
*when* :SESSIONS: gets written (detach from per-turn
Stop/UserPromptSubmit boundaries, write only at genuine session
close) — nothing about :LOGBOOK:'s intentionally fine-grained CLOCK
writes changes there. This proposal instead changes *what happens* to
writes that continue to happen at the current frequency, compacting
them immediately after each clock-out. If "Coarsen" ships first,
there's less to consolidate per day in the first place, but
:LOGBOOK: rounding/merging here would still apply regardless — the
two are complementary, not competing, and could both land.

*Risk:* rounding only ever touches already-closed history, so it can
never corrupt a running clock — the retrospective run and its tests
already exercise that exact boundary. The main remaining risk is
purely mechanical: forgetting to scope the on-the-fly call to *only*
the heading that was just clocked out of.


** Observability

Broader than clocking — visibility into what every tool call
actually did, both after the fact and at the start of a session.

** Bigger swings

Both about the standalone/Warp-access subsystem from this session.
Think hard before building either.

*** MAYBE Remote MCP access :code:
Off-machine access to the tools server and IDE-companion. This
session already got a =claude= CLI running directly in a Warp pane
on the same machine to reach Emacs (HTTP tools server +
IDE-companion lockfile, no vterm required). The obvious next step
is the same thing from *off-machine* — phone, another computer, a
CI job — but that means opening those servers beyond localhost,
which is real attack surface: arbitrary elisp execution against
your Emacs, today gated only by "it's on localhost." Don't build
this without designing auth first — bearer token at minimum, ideally
scoped per tool.

**** Plan [/]
- [ ] Add a bearer-token check to
  =claude-code-ide-mcp-http-server--handle-post= (new defcustom,
  nil-by-default = off) that short-circuits to a 401 before the
  request body is even parsed
- [ ] Add a matching =authToken= field to the WebSocket IDE-companion's
  lockfile and validate it in =claude-code-ide-mcp--on-open= — today
  it blindly attaches any connecting socket by port match, no
  identity check at all
- [ ] Make the hardcoded ="127.0.0.1"= bind address in both servers a
  defcustom
- [ ] Thread the token through =.mcp.json='s headers map on the
  client side rather than the URL, which tends to leak into logs
- [ ] Keep both servers bound to =127.0.0.1= permanently regardless,
  and require a tunnel (Tailscale/WireGuard/SSH) for actual remote
  reachability, with the token as a second factor

*Risk:* every registered tool call is unsandboxed elisp execution in
your live Emacs — a bearer token alone adds no TLS, per-tool
scoping, or rate limiting. Don't just flip the host to 0.0.0.0 and
call it done.

*** MAYBE Package the Warp wiring :code:
As a reusable snippet, not a private hack. Per the earlier scope
conversation: the port-pinning + session-registration +
IDE-companion-autostart block currently lives in personal
config.el, hand-rolled against claude-code-ide's internals rather
than a public API. Short of a real upstream PR, even just
generalizing it (hardcoded project path → variable) and writing it
up would make it something worth handing to someone else.

**** Plan [/]
- [ ] Generalize the hardcoded project path into an alist of
  (session-id . project-dir) pairs
- [ ] Turn the wiring function into a loop over that alist — both
  underlying calls are already idempotent, so re-running is safe
- [ ] Extract the whole thing out of personal config.el into a
  standalone file (e.g. =claude-code-ide-standalone.el=)
- [ ] Add a helper that generates each project's =.mcp.json= entry
  from the same alist instead of hand-writing the port/session-id
- [ ] Decide packaging tier — documented snippet, standalone package,
  or upstream PR — and stop there for now

*Open question:* the wrapped functions (=register-session=,
=mcp-start=) aren't really designed as public standalone-mode API
today, so an upstream PR is a bigger lift than it looks. Start with
the snippet, treat upstreaming as a separate future MAYBE.
